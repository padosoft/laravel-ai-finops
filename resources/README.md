# README assets

The root `README.md` references two images. Drop the provided artwork here at these exact paths:

- `resources/banner.png` — wide hero banner (shown after the badges, before the TOC).
- `resources/screenshots/admin-dashboard.png` — admin dashboard screenshot (Web Admin Panel section).

PNGs are tracked as binary (see `.gitattributes`).
