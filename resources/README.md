# README assets

The root `README.md` references two images, committed here:

- `resources/laravel-ai-finops-banner.png` — wide hero banner (shown after the badges, before the TOC).
- `resources/screenshoots/Ai-Finops-Web-Panel-Dashboard-Dark.png` — admin dashboard screenshot (Web Admin Panel section).

PNGs are tracked as binary (see `.gitattributes`).
